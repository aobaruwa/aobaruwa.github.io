# Local Website Test

From the `Portfolio` folder, start a local server:

```sh
python3 -m http.server 8000
```

Open [http://localhost:8000/test/](http://localhost:8000/test/). The test bench checks the homepage, stylesheet, script, and resume, then displays the real homepage in an iframe.